cc_library(
  name = "lib",
  srcs = glob(["**/*.c"]),
  hdrs = glob(["**/*.h"]),
  deps = ["//achelper", "//proto"],
  #copts = ["-m32", "-g"],
  # copts = ["-fsanitize=address", "-m32", "-g"],
  #linkopts = ["-lasan"],
  visibility = ["//:__subpackages__"],
)
